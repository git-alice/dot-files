# Extend Existing Zshrc with a Managed Source Block

The Bootstrap extends the existing `~/.zshrc` by adding or updating only a clearly marked source block that loads the Managed Config Directory. This preserves any current shell behavior while still making the Zsh Dotfiles easy to install, rerun, update, and remove.
